# Libraries that get copied onto the board, as circup module names.
# circup pulls each module's dependencies automatically.
#
# Installed into lessons\lib (NOT straight onto the board) so the repo stays the
# source of truth and a board can be rebuilt offline:
#
#   circup --path .\lessons --board-id matrixportal_m4 --cpy-version 10.2.1 \
#          install -r device-requirements.txt
#
# Keep this list in step with requirements-intellisense.txt.

adafruit_matrixportal
adafruit_display_text
adafruit_bitmap_font
adafruit_lis3dh
neopixel

# NOT adafruit_led_animation. It was tried for its colour names and dropped:
# 25 KB and 18.6 ms of strip-animation machinery for a list of constants.
# lessons\colors.py replaces it, and rainbowio.colorwheel is already in the
# firmware.
